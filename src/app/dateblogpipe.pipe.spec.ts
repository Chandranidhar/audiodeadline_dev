import { DateblogpipePipe } from './dateblogpipe.pipe';

describe('DateblogpipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateblogpipePipe();
    expect(pipe).toBeTruthy();
  });
});
