import { UserdatesearchPipe } from './userdatesearch.pipe';

describe('UserdatesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UserdatesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
